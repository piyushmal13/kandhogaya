The Antigravity Engine:
- All buttons use `SovereignButton`
- CSS is driven by `src/config/designTokens.ts`
- Feature flags pull from `platform_flags` via `useFeatureFlag` hook.
- All routing uses React Router v7.
